import _extends from "@babel/runtime/helpers/extends";
import React from 'react';
import { getDisplayName } from '@ui-schema/ui-schema';
// @ts-ignore
export const TableContext = /*#__PURE__*/React.createContext({});
export function withTable(Component) {
  const WithTable = p => {
    const {
      errors,
      valid
    } = useTable();
    return /*#__PURE__*/React.createElement(Component, _extends({
      valid: valid,
      errors: errors
    }, p));
  };

  WithTable.displayName = "ExtractValidity(".concat(getDisplayName(Component), ")");
  return WithTable;
}
export const useTable = () => React.useContext(TableContext);