"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertStringToNumber = void 0;

var _schemaTypeIs = require("@ui-schema/ui-schema/Utils/schemaTypeIs");

var convertStringToNumber = function convertStringToNumber(value, type) {
  if ((0, _schemaTypeIs.schemaTypeIsNumeric)(type)) {
    if (isNaN(value * 1)) {
      console.error('Invalid Type: input not a number in');
      return undefined;
    }

    return value === '' ? '' : value * 1;
  }

  return value;
};

exports.convertStringToNumber = convertStringToNumber;